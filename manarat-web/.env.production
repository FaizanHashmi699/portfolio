# Supabase publishable keys.
#
# These are designed to be public — the anon key ships inside the browser bundle
# either way. Every table is protected by row-level security, so this key can
# read published content and submit a form, and nothing else. Verified: an
# anonymous reader gets 0 rows from donations, enquiries and subscribers.
#
# The secret service-role key is deliberately NOT used anywhere in this app.
NEXT_PUBLIC_SUPABASE_URL=https://kfbldlpeeomsodqobgag.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_lWSKp8wIpG8W2_w1NP7JvQ_RYyjPA2m
